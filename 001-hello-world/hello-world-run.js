const helloWorld = require('./hello-world.js');

const result = helloWorld();

console.log(result);
// => Hello, World! (printed to the console)
// We need to right click on the file and select "Run Code" to see the output in the console.