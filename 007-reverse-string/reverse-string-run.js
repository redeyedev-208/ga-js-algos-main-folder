// To execute the other functions in the reverse-string.js file, we need to require it in this file.
// We can then call the function and pass in a string to see the result.
// We just need to uncomment the function we want to run and comment out the others.
const reverseString = require('./reverse-string.js');

const result = reverseString('coffee is the best drink in the world');

console.log(result);
